<Window
    x:Class="Level_Exporter.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Level_Exporter.Views"
    xmlns:converters="clr-namespace:Level_Exporter"
    xmlns:res="clr-namespace:Level_Exporter.Properties"
    xmlns:resources="clr-namespace:Level_Exporter.Resources"
    xmlns:resx ="clr-namespace:Level_Exporter.Resources"
    Title="{x:Static res:Resources.Title}"
    Width="800"
    Height="450"
    Name="MainWindow"
    x:Uid="MainWindow">

    <Window.Resources>
        <ResourceDictionary>

            <!-- Converts string name of asset to image source -->
            <converters:StringToImageSourceConverter
                x:Key="StringToImageSourceConverter" />

            <Style
                x:Key="AppButtonStyle"
                TargetType="{x:Type Button}">
                <Setter
                    Property="Margin"
                    Value="10" />
                <Setter
                    Property="Width"
                    Value="65" />
                <Setter
                    Property="Height"
                    Value="50" />
                <Setter
                    Property="Background"
                    Value="Transparent" />
            </Style>
        </ResourceDictionary>

    </Window.Resources>

    <Grid
        x:Name="MainGrid"
        x:Uid="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition
                Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel
            x:Uid="TextBlockStackPanel"
            Grid.Row="0"
            HorizontalAlignment="Right"
            VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
            <StackPanel.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform X="-25"/>
                </TransformGroup>
            </StackPanel.RenderTransform>
            <TextBlock
                Text="{x:Static resx:WindowStrings.CadTypePrompt }"
                FontSize="32"
                Width="300" RenderTransformOrigin="0.493,0.585" />
            <ComboBox>
                <ComboBoxItem FontSize="16" 
                              Content="{x:Static resources:WindowStrings.CadTypeStl}"/>
                <ComboBoxItem FontSize="16"
                              Content="{x:Static resources:WindowStrings.CadTypeStp}"/>
            </ComboBox>

        </StackPanel>

        <StackPanel Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center">
            
        </StackPanel>

        <StackPanel
            x:Name="ButtonPanel"
            x:Uid="ButtonStackPanel"
            Grid.Row="1"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom">
            <Button
                x:Name="OkButton"
                x:Uid="OkButton"
                Style="{StaticResource AppButtonStyle}"
                Command="{Binding OkCommand}" Width="98">
                <StackPanel HorizontalAlignment="Left">
                    <Image
                        Height="24"
                        Width="24"
                        Source="{Binding OkResource, Converter={StaticResource StringToImageSourceConverter}}" />
                    <TextBlock FontSize="14">Export CAD</TextBlock>
                </StackPanel>

            </Button>
            <Button
                x:Name="CloseButton"
                x:Uid="CloseButton"
                Style="{StaticResource AppButtonStyle}"
                Command="{Binding CloseCommand}"
                CommandParameter="{Binding ElementName=MainWindow}">
                <Button.Content>
                    <Image
                        Height="24"
                        Width="24"
                        Source="{Binding CancelResource, Converter={StaticResource StringToImageSourceConverter}}" />
                </Button.Content>
            </Button>
        </StackPanel>

    </Grid>
</Window>
